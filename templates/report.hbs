# 变更影响测试报告

> **生成时间**: {{meta.generatedAt}}
> **项目**: {{meta.project}}
> **版本对比**: `{{meta.baseRef}}` → `{{meta.targetRef}}`
> **测试地址**: {{meta.baseUrl}}

---

## 1. 变更概要

| 指标 | 数值 |
|------|------|
| 变更文件数 | {{diff.filesChanged}} |
| 新增行数 | +{{diff.additions}} |
| 删除行数 | -{{diff.deletions}} |

---

## 2. 影响分析

**风险等级**: {{analysis.riskLevel}}

**摘要**: {{analysis.summary}}

### 受影响页面
{{#each analysis.affectedPages}}
#### {{this.name}} (`{{this.route}}`)

- **变更类型**: {{this.changeType}}
- **变更描述**: {{this.changeDescription}}
- **关联文件**: {{#each this.impactedFiles}}`{{this}}` {{/each}}

**建议测试场景**:
{{#each this.testScenarios}}
- **[{{this.priority}}]** {{this.name}}
  - 步骤: {{#each this.steps}}{{this}} → {{/each}}
  - 预期: {{this.expectedResult}}
{{/each}}

{{/each}}

**建议**: {{analysis.recommendation}}

---

## 3. 测试执行结果

| 指标 | 数值 |
|------|------|
| 生成用例数 | {{tests.generated}} |
| 通过 | {{tests.passed}} |
| 失败 | {{tests.failed}} |

### 用例详情
{{#each tests.details}}
- **{{this.testFile}}** ({{this.duration}}ms): {{this.status}}
  {{#each this.scenarios}}
  - {{this.status}}: {{this.name}}
    {{#if this.error}}
    ```
    {{this.error}}
    ```
    {{/if}}
  {{/each}}
{{/each}}

---

## 4. 结论

{{conclusion}}

---

*报告由 e2e-auto-test 自动生成*
